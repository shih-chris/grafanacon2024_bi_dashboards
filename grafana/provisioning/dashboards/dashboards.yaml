apiVersion: 1

providers:
  - name: local-folder
    orgId: 1
    type: file
    disableDeletion: true
    updateIntervalSeconds: 0
    options:
      path: /etc/grafana/provisioning/dashboards/community_dashboards
